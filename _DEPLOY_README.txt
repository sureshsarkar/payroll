v132 - corporate membership CHECKOUT page: two-column (plan + what's-included + trust | order summary), billing-aware prices, secure-payment signals. FIX: adds hidden 'billing' field so annual checkout stays annual at pay() (was defaulting monthly). Defensive ($billing ?? 'monthly') so works even without v129 controller. Replace 1 file, then: php artisan view:clear ; php artisan optimize:clear
